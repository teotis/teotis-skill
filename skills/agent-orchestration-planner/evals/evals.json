{
  "skill_name": "agent-orchestration-planner",
  "evals": [
    {
      "id": 1,
      "prompt": "我要并行重构三个独立模块：日志系统、认证中间件、数据导出管道。帮我做一个多 agent 编排方案，用 Claude Code 的 Agents View 来调度。",
      "expected_output": "Generates a complete orchestration kit with INDEX.md, package docs, package-graph.tsv, state.tsv, agent-prompts.md, orchestrate.sh, and 99-finalize. Uses tail-driven execution with orchestrate.sh advance as the only dispatch mechanism.",
      "files": [],
      "assertions": [
        {
          "text": "Creates the full kit structure: INDEX.md, packages/, launchers/, status/."
        },
        {
          "text": "INDEX.md includes Dependency Graph, Merge Strategy, Authorization, and Stop Conditions."
        },
        {
          "text": "Package prompts end with orchestrate.sh advance --from <package-id>, not inline scheduling."
        },
        {
          "text": "orchestrate.sh implements start/advance/status/retry/finalize subcommands."
        },
        {
          "text": "99-finalize is a finalize package that verifies, merges, and reports, not a passive audit."
        }
      ]
    },
    {
      "id": 2,
      "prompt": "帮我用 dispatch-claude-agents.sh 来跑几个任务。",
      "expected_output": "Does not present dispatch-claude-agents.sh as a primary entrypoint. Redirects to orchestrate.sh start as the script-based entry, or to agent-prompts.md as the manual entry. If dispatch-claude-agents.sh exists, it should be a thin wrapper calling orchestrate.sh.",
      "files": [],
      "assertions": [
        {
          "text": "Does not present dispatch-claude-agents.sh as a primary user entrypoint."
        },
        {
          "text": "Presents orchestrate.sh start or agent-prompts.md as the entry paths."
        },
        {
          "text": "If dispatch-claude-agents.sh is mentioned, it is described as a thin wrapper around orchestrate.sh."
        }
      ]
    },
    {
      "id": 3,
      "prompt": "帮我优化一下当前代码，不需要编排，直接改就行。",
      "expected_output": "Does not trigger the orchestration skill. Explains that this skill is only for explicit multi-agent orchestration requests, and offers to do the work directly or use another appropriate skill.",
      "files": [],
      "assertions": [
        {
          "text": "Does not generate orchestration kit materials for a simple direct-work request."
        },
        {
          "text": "Correctly identifies that the user did not ask for multi-agent orchestration."
        },
        {
          "text": "Offers to perform the work directly or suggest an alternative skill."
        }
      ]
    },
    {
      "id": 4,
      "prompt": "我需要一个完整编排方案：把 docs/ 目录的 Markdown 翻译成中英双语，同时把 API 文档从 OpenAPI 生成，还要跑一次全量 lint 检查。三个任务互不依赖。",
      "expected_output": "Creates three independent packages with no inter-dependencies (status dependency only), generates the orchestration kit with proper graph showing all three in wave 1, and ensures package branches own disjoint paths to avoid merge conflicts.",
      "files": [],
      "assertions": [
        {
          "text": "All three packages have dependency type 'status' with no code dependencies between them."
        },
        {
          "text": "Package-graph.tsv shows all three functional packages in wave 1."
        },
        {
          "text": "Each package's allowed paths are disjoint to avoid merge conflicts."
        },
        {
          "text": "orchestrate.sh launch logic can run all three in parallel up to ORCHESTRATION_MAX_PARALLEL."
        },
        {
          "text": "State ledger initializes all three as ready/pending for first wave."
        }
      ]
    },
    {
      "id": 5,
      "prompt": "帮我做多 agent 调度，状态账本要实时更新，worktree 每个包一个，最终自动收口合并到 main。",
      "expected_output": "Generates the orchestration kit with per-package worktrees, coordinator state.tsv as the scheduler source of truth, tail-driven advance calls, and 99-finalize authorized to merge integration branch back to mainline after verification.",
      "files": [],
      "assertions": [
        {
          "text": "Each functional package has its own worktree path in the graph."
        },
        {
          "text": "state.tsv is the machine-readable source of truth, not worktree-local status."
        },
        {
          "text": "Package prompts write coordinator status to the absolute status path, not inside the worktree."
        },
        {
          "text": "99-finalize is authorized to create integration branch, merge packages, verify, and merge to mainline."
        },
        {
          "text": "Cleanup only happens after every finalize step succeeds."
        }
      ]
    },
    {
      "id": 6,
      "prompt": "生成一个可用脚本触发的多 agent orchestration kit。要求脚本在 Claude Code 后台 session 启动失败或瞬退时能可靠降级，不要误以为任务还在跑。",
      "expected_output": "Generates an orchestration kit whose orchestrate.sh comes from the shared runtime template. It records launch logs, captures background session ids, checks claude logs after launch, marks missing session ids invalid, marks unreadable or exited sessions stale, and never unlocks downstream work from launched alone.",
      "files": [],
      "assertions": [
        {
          "text": "orchestrate.sh is copied from skills/agent-orchestration-planner/scripts/orchestrate-template.sh."
        },
        {
          "text": "Launch writes status/launch-<package-id>.log."
        },
        {
          "text": "The background session id is recorded in the agent column of state.tsv."
        },
        {
          "text": "A missing background session id marks the package invalid."
        },
        {
          "text": "Unreadable claude logs or immediate session disappearance marks the package stale."
        },
        {
          "text": "Downstream packages unlock only from completed/finalized, not launched."
        }
      ]
    },
    {
      "id": 7,
      "prompt": "帮我做多 agent 编排，但之前 state.tsv 经常被 agent 写坏。请让状态账本更稳。",
      "expected_output": "Uses script-owned state mutation. Package prompts instruct agents to update state through orchestrate.sh mark-state, not manual TSV editing. The generated runtime validates graph/state headers and row widths, supports repair-state and doctor, and reports status/ledger mismatches as invalid.",
      "files": [],
      "assertions": [
        {
          "text": "Package prompts explicitly say not to edit state.tsv manually."
        },
        {
          "text": "Package prompts use orchestrate.sh mark-state for completed, blocked, and finalized states."
        },
        {
          "text": "orchestrate.sh validates exact state.tsv header and 17-field rows, including failure recovery context columns."
        },
        {
          "text": "orchestrate.sh validates exact package-graph.tsv header and 10-field rows."
        },
        {
          "text": "orchestrate.sh includes repair-state and doctor commands."
        },
        {
          "text": "Markdown status and state.tsv mismatch blocks downstream dispatch."
        }
      ]
    },
    {
      "id": 8,
      "prompt": "外部 agent 说包完成了，但我怀疑 worktree 里有脏改动和越界文件。请让 99-finalize 更可靠。",
      "expected_output": "Generates a finalize package that treats agent reports as claims, verifies package branches, commit hashes, allowed paths, clean worktrees, recorded evidence, and integration merge order before merging. It blocks dirty/unrecorded state and verifies recorded package commits in a clean detached worktree when status and workspace disagree.",
      "files": [],
      "assertions": [
        {
          "text": "99-finalize verifies branch existence and recorded commit existence for each package."
        },
        {
          "text": "99-finalize checks changed files against allowed paths."
        },
        {
          "text": "99-finalize blocks dirty package worktrees unless explicitly recorded as a blocker."
        },
        {
          "text": "99-finalize verifies recorded commits in a clean detached worktree when status and workspace disagree."
        },
        {
          "text": "99-finalize does not mix coordinator ledger commits with unrelated orchestration docs or concurrent mainline edits."
        }
      ]
    },
    {
      "id": 9,
      "prompt": "我用脚本启动后只看到 `backgrounded · 2072b1cc`，之前账本把 `·` 当成 session id，还误判为失败。请生成不会再犯这个错误的 orchestration kit。",
      "expected_output": "Generates an orchestration kit whose shared orchestrate.sh parses Claude Code background session ids from both `backgrounded <id>` and `backgrounded · <id>`, stores the real id, writes launch logs, checks logs using the parsed id, and includes permission-mode guards for auto and bypassPermissions.",
      "files": [],
      "assertions": [
        {
          "text": "The generated runtime parses `backgrounded · <id>` and stores `<id>`, not `·`, in state.tsv."
        },
        {
          "text": "The launcher defaults to no explicit --permission-mode."
        },
        {
          "text": "CLAUDE_PERMISSION_MODE=auto requires CLAUDE_AUTO_MODE_OPTED_IN=1 after interactive opt-in."
        },
        {
          "text": "CLAUDE_PERMISSION_MODE=bypassPermissions requires CLAUDE_BYPASS_PERMISSIONS_APPROVED=1."
        },
        {
          "text": "The kit exposes `doctor --environment` for Claude CLI path/version and agents availability diagnostics."
        }
      ]
    },
    {
      "id": 10,
      "prompt": "这个编排里有些包必须人工处理，比如 public 发布、force-push 或安全审核；同时 99-finalize 不能只信 FINAL_REPORT。请让脚本和包设计支持这种情况。",
      "expected_output": "Generates package-graph rows that can use manual=1, never auto-launches manual packages, marks them manual_required when dependencies are satisfied, and requires 99-finalize to run verify-finalize before merging or claiming completion.",
      "files": [],
      "assertions": [
        {
          "text": "manual=1 packages are not launched by start/advance/retry automatically."
        },
        {
          "text": "When a manual package's dependencies are satisfied, state.tsv records manual_required."
        },
        {
          "text": "status output and final chat response identify concrete manual package ids."
        },
        {
          "text": "orchestrate.sh includes verify-package and verify-finalize commands."
        },
        {
          "text": "99-finalize runs verify-finalize and blocks missing branch, missing commit_hash, nonexistent commit, or dirty worktree evidence."
        }
      ]
    },
    {
      "id": 11,
      "prompt": "一个包反复因为同一个 Claude 启动错误失败，agent 一直 retry 导致空转。请让 orchestration kit 自动阻断这种重复失败。",
      "expected_output": "Generates an orchestration kit whose orchestrate.sh records terminal failures with normalized fingerprints and blocks retry after the same package hits the same fingerprint three times.",
      "files": [],
      "assertions": [
        {
          "text": "status/events.jsonl records terminal_failure events with package id, state, error, and fingerprint."
        },
        {
          "text": "retry checks previous terminal_failure events before resetting state."
        },
        {
          "text": "After three matching failures for the same package and fingerprint, retry stops before relaunching."
        },
        {
          "text": "The retry blocker prints the package id, failure count, and failure fingerprint for human diagnosis."
        }
      ]
    },
    {
      "id": 12,
      "prompt": "多 agent 跑完以后我需要知道到底发生过什么，不想只看最终 state.tsv。请让 orchestration kit 保留可审计事件流。",
      "expected_output": "Generates an orchestration kit with append-only status/events.jsonl covering launch requests, launch success, state changes, terminal failures, retry requests, and retry blocks.",
      "files": [],
      "assertions": [
        {
          "text": "The generated kit includes status/events.jsonl in its status directory design."
        },
        {
          "text": "orchestrate.sh appends launch_requested and launch_succeeded events with package id and session id."
        },
        {
          "text": "orchestrate.sh appends state_changed events for mark-state and internal state updates."
        },
        {
          "text": "orchestrate.sh appends retry_requested and retry_blocked events."
        },
        {
          "text": "99-finalize or failure reporting uses events.jsonl when explaining repeated failures."
        }
      ]
    },
    {
      "id": 13,
      "prompt": "多 agent 之间需要临时共享调查笔记、命令输出和中间清单，但这些东西不能污染状态账本，也不能被当作完成证据。请让 orchestration kit 支持这种临时共享材料区。",
      "expected_output": "Generates an orchestration kit with a plan-local gitignored scratch area and a scratch-path command that creates package-local scratch directories without changing scheduler truth.",
      "files": [],
      "assertions": [
        {
          "text": "The generated kit includes scratch/.gitignore in its directory structure."
        },
        {
          "text": "orchestrate.sh exposes scratch-path <package-id> and prints the absolute package scratch path."
        },
        {
          "text": "scratch-path creates the package scratch directory and records scratch_path_requested in events.jsonl."
        },
        {
          "text": "Package prompts say scratch is for temporary non-sensitive notes, inventories, logs, draft diffs, or intermediate artifacts only."
        },
        {
          "text": "Scratch contents do not unlock dependencies, satisfy acceptance criteria by themselves, or replace state.tsv/status evidence."
        },
        {
          "text": "Package prompts forbid credentials, tokens, private keys, .env files, hidden prompts, and proprietary raw data in scratch."
        }
      ]
    },
    {
      "id": 14,
      "prompt": "多 agent finalize 时 merge/test 失败以后不要盲目 retry。请把冲突文件、失败命令和日志摘要写进状态账本，让后续 agent 带着上下文修。",
      "expected_output": "Generates an orchestration kit whose state.tsv includes structured failure recovery context, whose mark-state command can record failed command, conflict files, log summary, and recovery hint, and whose retry path reports prior context before relaunching.",
      "files": [],
      "assertions": [
        {
          "text": "state.tsv includes last_error, failed_command, conflict_files, log_summary, and recovery_hint columns."
        },
        {
          "text": "mark-state supports --failed-command, --conflict-files, --log-summary, and --recovery-hint."
        },
        {
          "text": "blocked/stale/invalid package failures are recorded as terminal_failure events with recovery context."
        },
        {
          "text": "retry reports prior recovery context before resetting or relaunching a package."
        },
        {
          "text": "99-finalize records merge or test failures in the coordinator ledger, not only in prose."
        }
      ]
    },
    {
      "id": 15,
      "prompt": "我要多 agent 做 Android 相机修复，最后需要真机验收。Claude Code 没有我的手机，不能自己点真机 UI。请生成不会在真机验收环节卡死的 orchestration kit。",
      "expected_output": "Generates an orchestration kit that performs a capability preflight, keeps real-device validation out of autonomous Claude Code functional packages, provides external-assist instructions and evidence expectations up front, and avoids making non-autonomous validation a surprise dependency that blocks implementation waves.",
      "files": [],
      "assertions": [
        {
          "text": "The INDEX includes a Capability Preflight or External Assistance section that identifies real-device QA as non-autonomous for Claude Code."
        },
        {
          "text": "No auto-launched functional package is assigned to perform real-device tapping, visual inspection, external approvals, credentials entry, or other unavailable capabilities."
        },
        {
          "text": "Implementation packages use agent-verifiable checks such as unit tests, build commands, APK generation, logs, or emulator checks where appropriate."
        },
        {
          "text": "Real-device validation is represented as a predeclared external-assist checklist or manual release gate with owner, commands, expected evidence, and how results should be reported back."
        },
        {
          "text": "The graph does not make downstream implementation waves depend on non-autonomous validation unless the user explicitly marks it as a mandatory release gate."
        },
        {
          "text": "The final report is allowed to say ready for external QA rather than claiming PASS for unperformed real-device validation."
        }
      ]
    }
  ]
}
